# Synthetic Data Analysis

This folder contains scripts for generating synthetic data, training the DART model, and evaluating its performance.

## Scripts

- `gen_ideal.jl` – Generate **idealized synthetic data**.
- `gen_real.jl` – Generate **realistic synthetic data**.
- `apply_ref.jl` - Apply the trained DART on 3-state irreversible model.
- `dl_run_ideal.jl` – Train and evaluate DART on **idealized data**.
- `dl_run_real.jl` – Train and evaluate DART on **realistic data** with lognormal distributed noise (cv denotes coefficient of variation):
  - `cv0` – 5% technical noise
  - `cv1` – 10% technical noise
  - `cv2` – 20% technical noise

## Data

All generated data is stored in the `synthetic_data` folder (generated synthetic data is in Zenodo: https://doi.org/10.5281/zenodo.17100879).

### `base_compare`
Binarized promoter-state results from different methods (HMM, MA, SG) at three burstiness levels.  
Example trace for Fig. 4B,C: `dl_eg_traces.jld2`.

### `ideal_data_res`
Idealized data used for DART and other binarization methods comparison:
- Files: high bursintess level: `genb_mtest.jld2`, intermediate level: `gennb1_mtest.jld2`, low level: `gennb0_mtest.jld2` (generated by `gen_ideal.jl`)
- DART models:
  - `mtrained_modelb33_seed_s.bson`  
  - `mtrained_modelnb133_seed_s.bson`  
  - `mtrained_modelnb033_seed_s.bson`  
- DART test results:  
  - `mgen_dlb33_compare_seed_s.jld2`  
  - `mgen_dlb133_compare_seed_s.jld2`  
  - `mgen_dlb033_compare_seed_s.jld2`  
  *(s = 1,2,...,100)* 
- Hyperparameter tuning: change `33` → `333` 
> All results are obtained by running `dl_run_ideal.jl`.
- Results of applying DART to a different promoter-switching model: 3-state irreversible model (synthetic data, high burstiness level: `refb_mtest.jld2`)  
  - Output file: `mref_dlb33_compare_seed_58.jld2`

### Realistic Data
Realistic data used for DART:

- `real_cv0_data_res` – cv0 (5% noise)
- `real_cv1_data_res` – cv1 (10% noise)
- `real_cv2_data_res` – cv2 (20% noise)

Each folder contains:

- **Synthetic data**  
  `genb_ntest_cvX.jld2`, `gennb1_ntest_cvX.jld2`, `gennb0_ntest_cvX.jld2`  
  *(generated by `gen_real.jl`)*

- **DART trained models**  
  - With noise: `ntrained_modelb_seed_1.bson`, `ntrained_modelnb1_seed_1.bson`, `ntrained_modelnb0_seed_1.bson`  
  - Without noise: `mtrained_modelb_seed_1.bson`, `mtrained_modelnb1_seed_1.bson`, `mtrained_modelnb0_seed_1.bson`

- **DART test results**  
  - With noise: `ngen_dlb_compare_seed_1.jld2`, `ngen_dlnb1_compare_seed_1.jld2`, `ngen_dlnb0_compare_seed_1.jld2`  
  - Without noise: `mgen_dlb_compare_seed_1.jld2`, `mgen_dlnb1_compare_seed_1`, `mgen_dlnb0_compare_seed_1.jld2`

> Note: `n` indicates results with added noise, while `m` indicates the noise-free case. All results are obtained by running `dl_run_real.jl`.

Realistic data used for comparison of DART, burstInfer, and standard HMM
- `bI_compare_cv0` – cv0 (5% noise)
- `bI_compare_cv2` – cv2 (20% noise)

